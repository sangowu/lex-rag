# Environment & secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/
.eggs/

# Data (large files & downloaded datasets)
data/cuad_docs/
data/legalbench_docs/
# 合成扫描件：2MB 的二进制，且由 make_scanned_demo.py 的固定种子完全可复现，
# 进库只会让 clone 变大。
data/scanned_docs/
data/runs/
data/omnidocbench_annotations.json
data/embed_cache*.pkl
data/*.pkl
data/*.jsonl
# 回归集是**源码不是产物**：手工挑的 16 条案例，tests/test_gate.py 直接读它。
# 被 data/*.jsonl 一并忽略掉的话，本地全绿而 CI 里 FileNotFoundError。
!data/regression_set.jsonl

# LLM caches
.cache/

# Tooling caches
.ruff_cache/
.pytest_cache/
tests/.tmp_test_artifacts/

# Claude Code local settings
.claude/

# IDE
.vscode/
.idea/

# Deploy scratch files
deploy/.tg_arn
